<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Library George</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
</head>
<body>
  </div>
  <div class="container">

    <div class="row">
      <div class="col-md-6">
        <h3 class="mt-4">Añadir un nuevo libro</h3>
        <form action="/book" method="POST">
          <%- include('_form_field') %>
        </form>
        </div>

        <div class="col-md-6">
          <h3 class="mt-4">Save books</h3>
          <table>
            <tr>
              <td>Name Book</td>
              <td>Author</td>
              <td>Category</td>
              <td>Date</td>
              <td>User</td>
              <td>Actions</td>
            </tr>

            <% books.forEach(book => { %>
            <tr>
                <td> <%= book.name %> </td>
                <td> <%= book.author %> </td>
                <td> <%= book.category %> </td>
                <td> <%= book.date %> </td>
                <td> <%= book.user %> </td>
                <td>
                  <button class="btn btn-danger">Delete</button>
                  <button class="btn btn-secondary">Edit</button>
                </td>
             <% }) %>
            </tr>
            
          </table>
       
        </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>